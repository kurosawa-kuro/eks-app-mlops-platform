/**
 * Shop Routes - API routes only
 *
 * Note: Page routes (pages.ts) have been removed.
 * Frontend is now handled by Next.js (app-frontend).
 *
 * Includes:
 * - Product browsing, cart, and orders API
 * - User authentication API (login, logout, refresh)
 */

export { shopRoutes } from './shop.js'
export { authRoutes } from './auth.js'
export { shopApi } from './shopApi.js'
