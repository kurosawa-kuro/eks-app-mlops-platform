# AWS Resource Monitor Backend ― 実装状況サマリ

> **対象ユースケース**: PCデスクトップ向け管理画面・業務アプリケーション
> スマートフォン向けのレスポンシブデザインは考慮されていません。

---

## 技術スタック

| カテゴリ | 技術 |
|---------|------|
| Runtime | Node.js |
| Framework | Hono v4 |
| Database | MongoDB (mongodb v7) |
| Cache/Queue | Redis (Upstash @upstash/redis) - Optional |
| DI | Awilix v12 |
| Validation | Zod v4 |
| Logging | Pino v10 |
| Template | EJS |
| 認証 | Clerk (@hono/clerk-auth) |
| ファイルアップロード | Cloudinary v2 |
| AWS SDK | @aws-sdk/client-* v3（19サービス対応） |
| Kubernetes | @kubernetes/client-node v1 |
| Test | Vitest v4, Playwright |
| Secret管理 | Doppler |

---

## ディレクトリ構成

```
src/
├── index.ts              # エントリーポイント（サーバ起動）
├── app.ts                # Hono アプリケーション生成
├── env/                  # 環境設定
│   ├── schema.ts         # Zodスキーマ (AppConfig)
│   ├── loader.ts         # 設定読み込み (env + file)
│   └── index.ts          # エクスポート
├── shared/               # 共通ユーティリティ（レイヤー非依存）
│   ├── logger.ts         # Pino ロガー設定
│   ├── errors.ts         # AppError, Errorsファクトリー
│   ├── validators.ts     # Zodバリデーションヘルパー
│   ├── requestId.ts      # リクエストID生成
│   └── authAuditLogger.ts # 認証監査ログ
├── contracts/            # API契約層（LTS v1）
│   └── lts-v1/
│       ├── documents/    # 凍結された型定義（Micropost, Settings）
│       ├── types/        # 共通型（ID, Timestamp）
│       ├── adapters/     # ドメイン ↔ 契約変換
│       └── index.ts      # 公開API
├── di/
│   └── container.ts      # Awilix DIコンテナ設定（全依存を一元管理）
├── domain/               # ドメイン層（業務ロジック・抽象）
│   ├── entities/         # エンティティ定義
│   │   ├── micropost.ts  # マイクロポスト
│   │   ├── settings.ts   # 設定
│   │   ├── resource.ts   # AWSリソース
│   │   ├── cost.ts       # コスト
│   │   └── k8s.ts        # Kubernetes
│   ├── repositories/     # リポジトリインターフェース
│   ├── services/         # サービスインターフェース
│   ├── usecases/         # ユースケース
│   └── auth/             # 認証・認可ドメイン (AUTH FEATURE)
├── infra/                # インフラ層（外部実装）
│   ├── aws/              # AWS統合
│   │   ├── adapters/     # AWSサービスアダプター（19個）
│   │   ├── resourceService.ts   # リソース監視サービス
│   │   ├── costService.ts       # コスト分析サービス
│   │   └── eksSystemlogService.ts # EKSシステムログサービス
│   ├── k8s/              # Kubernetes統合
│   │   ├── adapters/     # K8sアダプター
│   │   │   ├── kubernetes.adapter.ts      # 直接接続
│   │   │   ├── kubernetes-ssm.adapter.ts  # SSM Bastion経由
│   │   │   └── prometheus.adapter.ts      # Prometheusメトリクス
│   │   ├── k8sService.ts        # K8sワーカーサービス
│   │   └── prometheusService.ts # Prometheusワーカーサービス
│   ├── cache/            # キャッシュ実装
│   │   └── memoryCache.ts
│   ├── database/
│   │   ├── clients/      # DBクライアント (mongo.ts, redis.ts)
│   │   └── errors.ts     # wrapDbOperation
│   ├── repositories/     # リポジトリ実装
│   └── services/         # サービス実装
│       ├── fileUpload.ts
│       └── jobStore/     # memory.ts, redis.ts
└── presentation/         # プレゼンテーション層（HTTP/UI）
    ├── routes/           # ルート定義（機能別 page.ts/api.ts 分離）
    │   ├── api/          # 公開API (health.ts)
    │   ├── auth/         # 認証ルート (page.ts, api.ts)
    │   ├── aws/          # AWSルート (page.ts, api.ts)
    │   ├── k8s/          # K8sルート (page.ts, api.ts)
    │   ├── dashboard/    # ダッシュボード
    │   │   └── micropost/ # マイクロポスト (page.ts, api.ts)
    │   ├── settings/     # 設定 (page.ts, api.ts)
    │   ├── system/jobs/  # ジョブ (page.ts, api.ts)
    │   ├── admin/        # 管理画面
    │   └── devtool/      # 開発ツール
    ├── controllers/      # コントローラ
    │   ├── aws/          # AWSコントローラ (api.ts, page.ts)
    │   ├── k8s/          # K8sコントローラ (api.ts, page.ts)
    │   └── ...           # その他コントローラ
    ├── middleware/       # ミドルウェア
    │   └── auth/         # 認証ミドルウェア (clerk.ts, helpers.ts)
    ├── schemas/          # リクエストスキーマ
    ├── helpers/          # ヘルパー
    │   ├── render.ts     # EJSレンダリング
    │   ├── awsFormatters.ts  # AWSデータフォーマッター
    │   ├── pageController.ts # ページコントローラファクトリ
    │   └── apiController.ts  # APIコントローラファクトリ
    ├── views/            # EJSテンプレート
    │   ├── layouts/      # レイアウト (main.ejs, aws-main.ejs)
    │   ├── aws/          # AWSダッシュボード用ビュー
    │   ├── k8s/          # K8sダッシュボード用ビュー
    │   └── ...           # その他ビュー
    └── public/           # 静的ファイル

env/
└── config.json           # ConfigMap (非機密設定)

test/
├── integration/          # 統合テスト (3ファイル)
├── unit/                 # ユニットテスト (51ファイル)
│   ├── auth/             # 認証テスト
│   ├── aws/              # AWSテスト
│   ├── controllers/      # コントローラテスト
│   ├── di/               # DIコンテナテスト
│   ├── env/              # 環境設定テスト
│   ├── helpers/          # ヘルパーテスト
│   ├── infra/            # インフラ層テスト
│   ├── middleware/       # ミドルウェアテスト
│   ├── routes/           # ルートテスト
│   ├── schemas/          # スキーマテスト
│   ├── services/         # サービステスト
│   ├── shared/           # 共通ユーティリティテスト
│   └── usecases/         # ユースケーステスト
└── helpers/              # テストヘルパー

e2e/
└── example.spec.ts       # E2Eテスト例
```

---

## 環境設定 (src/env/)

### 実装状態

| 項目 | 状態 |
|------|------|
| Zodスキーマ定義 | 実装済み |
| ConfigMapファイル読み込み | 実装済み (`env/config.json`) |
| 環境変数読み込み | 実装済み |
| 優先順位: 環境変数 > ファイル | 実装済み |
| K8s ConfigMap対応 (`CONFIG_FILE_PATH`) | 実装済み |

### 設定項目

#### ① 実行モード / 環境制御

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| appEnv | enum | `APP_ENV` | local/development/production |
| appMode | enum | `APP_MODE` | origin/stable |

#### ② サーバー / アプリ基本設定

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| projectName | string | `PROJECT_NAME` | デフォルト: 'aws-resource-monitor' |
| port | number | `PORT` | デフォルト: 8001 |
| logLevel | enum | `LOG_LEVEL` | debug/info/warn/error |
| rateLimitPerMinute | number | `RATE_LIMIT_PER_MINUTE` | デフォルト: 100 |

#### ③ 認証・認可（Clerk / 内部API）

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| authEnabled | boolean | `AUTH_ENABLED` | Clerk認証の有効/無効 |
| authJwtSecret | string | `AUTH_JWT_SECRET` | JWT署名用シークレット |
| authClerkPublishableKey | string | `AUTH_CLERK_PUBLISHABLE_KEY` | Clerk Publishable Key |
| authClerkSecretKey | string | `AUTH_CLERK_SECRET_KEY` | Clerk Secret Key |

#### ③-2 内部通知API

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| notifyInternalApiUrl | string | `NOTIFY_INTERNAL_API_URL` | 通知API URL |
| notifyInternalApiKey | string | `NOTIFY_INTERNAL_API_KEY` | 通知APIキー |

#### ④ データベース

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| dbMongodbUri | string | `DB_MONGODB_URI` | MongoDB接続URI |
| dbNeonUri | string | `DB_NEON_URI` | Neon PostgreSQL URI（将来用） |
| mongoDbName | string | `DB_MONGODB_DB_NAME` | デフォルト: 'starter' |

#### ⑤ AWS（Read / 管理系）

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| awsAccessKeyId | string | `AWS_ACCESS_KEY_ID` | AWS IAM Access Key |
| awsSecretAccessKey | string | `AWS_SECRET_ACCESS_KEY` | AWS IAM Secret Key |
| awsRegion | string | `AWS_REGION` | デフォルト: 'ap-northeast-1' |

#### ⑤-2 AWS Resource Monitor キャッシュ設定

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| cacheTtlResources | number | `CACHE_TTL_RESOURCES` | リソースキャッシュTTL（デフォルト: 300000=5分） |
| cacheTtlCosts | number | `CACHE_TTL_COSTS` | コストキャッシュTTL（デフォルト: 3600000=1時間） |

#### ⑤-3 Kubernetes

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| k8sNamespace | string | `K8S_NAMESPACE` | 空=全namespace |
| k8sMonitoredNamespaces | string | `K8S_MONITORED_NAMESPACES` | 監視対象namespace（カンマ区切り） |
| k8sBastionInstanceId | string | `K8S_BASTION_INSTANCE_ID` | SSM経由アクセス用BastionインスタンスID |
| k8sEksClusterName | string | `K8S_EKS_CLUSTER_NAME` | EKSクラスター名 |
| prometheusUrl | string | `PROMETHEUS_URL` | Prometheus URL |
| lokiUrl | string | `LOKI_URL` | Loki URL |
| cacheTtlK8s | number | `CACHE_TTL_K8S` | K8sキャッシュTTL（デフォルト: 60000=1分） |
| cacheTtlMetrics | number | `CACHE_TTL_METRICS` | メトリクスキャッシュTTL（デフォルト: 30000=30秒） |

#### ⑥ GitHub / CI・自動化

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| gitGithubAccessToken | string | `GIT_GITHUB_ACCESS_TOKEN` | GitHub API Token |

#### ⑦ AI / LLM（共通）

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| aiOpenaiApiKey | string | `AI_OPENAI_API_KEY` | OpenAI API Key |
| aiAnthropicApiKey | string | `AI_ANTHROPIC_API_KEY` | Anthropic API Key |
| aiDeepseekApiKey | string | `AI_DEEPSEEK_API_KEY` | DeepSeek API Key |
| aiKaggleToken | string | `AI_KAGGLE_TOKEN` | Kaggle Token |

#### ⑧ LLM-GW（防御用）

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| llmGwEnabled | boolean | `LLM_GW_ENABLED` | LLM Gateway有効/無効 |
| llmGwProjectApiKey | string | `LLM_GW_PROJECT_API_KEY` | プロジェクトAPIキー |
| llmGwMaxTokensPerRequest | number | `LLM_GW_MAX_TOKENS_PER_REQUEST` | リクエスト毎最大トークン |
| llmGwDailyTokenLimit | number | `LLM_GW_DAILY_TOKEN_LIMIT` | 日次トークン制限 |
| llmGwMonthlyTokenLimit | number | `LLM_GW_MONTHLY_TOKEN_LIMIT` | 月次トークン制限 |
| llmGwRateLimitPerMinute | number | `LLM_GW_RATE_LIMIT_PER_MINUTE` | 分間レート制限 |
| llmGwRateLimitPerHour | number | `LLM_GW_RATE_LIMIT_PER_HOUR` | 時間レート制限 |

#### ⑨ アラート / 監視

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| alertEnabled | boolean | `ALERT_ENABLED` | アラート有効/無効 |
| alertChannel | enum | `ALERT_CHANNEL` | slack/email |
| alertSlackWebhookUrl | string | `ALERT_SLACK_WEBHOOK_URL` | Slack Webhook URL |
| alertEmailTo | string | `ALERT_EMAIL_TO` | アラート送信先メール |

#### ⑩ Render / デプロイ補助

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| renderServiceName | string | `RENDER_SERVICE_NAME` | Renderサービス名 |
| renderEnvironment | string | `RENDER_ENVIRONMENT` | Render環境名 |

#### ⑪ 内部・運用専用

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| internalDeploySshPrivateKey | string | `INTERNAL_DEPLOY_SSH_PRIVATE_KEY` | デプロイ用SSH秘密鍵 |

#### ⑫ メディア（Cloudinary）

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| mediaCloudinaryCloudName | string | `MEDIA_CLOUDINARY_CLOUD_NAME` | Cloudinary設定 |
| mediaCloudinaryApiKey | string | `MEDIA_CLOUDINARY_API_KEY` | Cloudinary設定 |
| mediaCloudinaryApiSecret | string | `MEDIA_CLOUDINARY_API_SECRET` | Cloudinary設定 |

#### ⑬ Redis（Optional - Upstash）

| キー | 型 | 環境変数 | 備考 |
|------|-----|--------|------|
| redisUrl | string | `REDIS_URL` | Upstash Redis URL |
| redisToken | string | `REDIS_TOKEN` | Upstash Redis Token |

### 読み込み優先順位

1. 環境変数 (Doppler, K8s Secret envFrom)
2. ConfigMapファイル (`env/config.json` or `CONFIG_FILE_PATH`)

Secret類は環境変数のみ対応（ファイル非対応）。

### env/config.json

開発環境向けのデフォルト設定ファイル。Secret以外の設定を格納。

```json
{
  "projectName": "aws-resource-monitor",
  "appEnv": "local",
  "appMode": "origin",
  "port": 8001,
  "logLevel": "debug",
  "mongoDbName": "aws-resource-monitor",
  "authEnabled": false,
  "alertEnabled": false,
  "rateLimitPerMinute": 1000,
  "k8sEnabled": true,
  "k8sBastionInstanceId": "i-0eaab0b86cccfaa47",
  "k8sEksClusterName": "prod-eks-cluster"
}
```

本番環境ではDopplerまたはK8s ConfigMapで管理。

---

## 認証機能 (AUTH FEATURE)

### 概要

Clerk による認証機能。`AUTH_ENABLED=true` で有効化。

### ロール階層

| Role | Level | 説明 |
|------|-------|------|
| owner | 100 | 全権限（システム管理含む） |
| admin | 75 | 管理者権限 |
| operator | 50 | 操作権限（設定変更可） |
| viewer | 25 | 閲覧のみ |

### ロール解決の優先順位

1. Clerk `sessionClaims.publicMetadata.role` (有効なロールの場合)
2. `ownerAdminUserIds` に含まれるユーザー → `owner`
3. デフォルト → `viewer`

### 認証ルート

| パス | 説明 |
|------|------|
| `/auth/login` | ログインページ（Clerk UI） |
| `/auth/status` | 認証ステータス API |
| `/auth/me` | 現在のユーザー情報 API |

### ミドルウェア

| 関数 | ファイル | 説明 |
|------|----------|------|
| `createAuthMiddleware` | `auth/clerk.ts` | Clerkセッション検証 |
| `createRequireAuthMiddleware` | `auth/clerk.ts` | 認証必須（未認証→ログインへリダイレクト） |
| `createAuthContextMiddleware` | `auth/clerk.ts` | AuthContext構築（ロール解決含む） |
| `createAuthorizeMiddleware` | `auth/clerk.ts` | ポリシーベースのアクセス制御 |
| `createAuthStrategy` | `auth/helpers.ts` | 認証ストラテジー生成 |
| `authRequired` | `auth/helpers.ts` | 認証必須ミドルウェアチェーン |
| `authOptional` | `auth/helpers.ts` | 認証任意ミドルウェアチェーン |

### アクセスポリシー (canAccess)

| ポリシー | 必要ロール |
|----------|-----------|
| `canAccess.dashboard` | owner のみ |
| `canAccess.settingsRead` | viewer+ |
| `canAccess.settingsWrite` | operator+ |
| `canAccess.adminPage` | admin+ |
| `canAccess.devtool` | admin+ |
| `canAccess.system` | owner のみ |
| `canAccess.jobsOperate` | operator+ |
| `canAccess.jobsView` | viewer+ |
| `canAccess.authenticated` | 認証済みユーザー全員 |

### 関連ファイル

- `src/domain/auth/types.ts` - ロール定義、AuthContext型
- `src/domain/auth/policies.ts` - ポリシー関数（hasMinRole, allOf, anyOf, not）
- `src/domain/auth/roleResolver.ts` - ロール解決ロジック
- `src/presentation/middleware/auth/clerk.ts` - 認証ミドルウェア
- `src/presentation/middleware/auth/helpers.ts` - authRequired(), authOptional()
- `src/presentation/middleware/auth/index.ts` - re-export
- `src/presentation/routes/auth/` - 認証ルート (page.ts, api.ts)
- `src/shared/authAuditLogger.ts` - 監査ログ

詳細は [01-clerk認証.md](../04-機能ガイド/01-clerk認証.md) 参照。

---

## AWS統合機能

### 概要

AWS SDKを使用してAWSリソースの監視とコスト分析を行う機能。

### 対応AWSサービス（19個）

| サービス | アダプターファイル | 説明 |
|----------|-------------------|------|
| ACM | `acm.adapter.ts` | AWS Certificate Manager |
| Auto Scaling | `autoscaling.adapter.ts` | Auto Scalingグループ |
| CloudWatch Logs | `cloudwatchlogs.adapter.ts` | ログ管理 |
| Cost Explorer | `cost.adapter.ts` | コスト分析 |
| EC2 | `ec2.adapter.ts` | 仮想マシン |
| ECR | `ecr.adapter.ts` | コンテナレジストリ |
| EKS | `eks.adapter.ts` | Kubernetesサービス |
| ElastiCache | `elasticache.adapter.ts` | キャッシュ |
| ELB v2 | `elb.adapter.ts` | ロードバランサー |
| Firehose | `firehose.adapter.ts` | データストリーム |
| IAM | `iam.adapter.ts` | アクセス管理 |
| KMS | `kms.adapter.ts` | 暗号化キー管理 |
| Lambda | `lambda.adapter.ts` | サーバーレス関数 |
| RDS | `rds.adapter.ts` | リレーショナルDB |
| Route 53 | `route53.adapter.ts` | DNS |
| S3 | `s3.adapter.ts` | オブジェクトストレージ |
| SQS | `sqs.adapter.ts` | メッセージキュー |
| SSM | `ssm.adapter.ts` | Systems Manager |
| VPC | `vpc.adapter.ts` | 仮想ネットワーク |

### AWSサービスファイル

| ファイル | 説明 |
|----------|------|
| `resourceService.ts` | リソース監視サービス（全AWSリソースの取得・集約） |
| `costService.ts` | コスト分析サービス（Cost Explorer連携） |
| `eksSystemlogService.ts` | EKSシステムログサービス |

### AWSルート

| パス | 説明 |
|------|------|
| `/aws` | AWSダッシュボード |
| `/aws/resources` | リソース一覧 |
| `/aws/costs` | コスト分析 |
| `/aws/alerts-high-cost` | 高コストアラート |
| `/aws/eks-overview` | EKS概要 |
| `/aws/eks-systemlog` | EKSシステムログ |
| `/aws/admin-info` | 管理者情報 |

---

## Kubernetes統合機能

### 概要

Kubernetes APIおよびPrometheus/Lokiと連携してクラスター監視を行う機能。

### K8sアダプター

| ファイル | 説明 |
|----------|------|
| `kubernetes.adapter.ts` | 直接K8s API接続 |
| `kubernetes-ssm.adapter.ts` | SSM Bastion経由でのK8s API接続 |
| `prometheus.adapter.ts` | Prometheusメトリクス取得 |

### K8sサービス

| ファイル | 説明 |
|----------|------|
| `k8sService.ts` | K8sワーカーサービス（Pod、Node、Deploymentの取得） |
| `prometheusService.ts` | Prometheusワーカーサービス（メトリクス集約） |

### K8sルート

| パス | 説明 |
|------|------|
| `/k8s` | K8sダッシュボード |
| `/k8s/api/pods` | Pod一覧API |
| `/k8s/api/nodes` | Node一覧API |
| `/k8s/api/metrics` | メトリクスAPI |

---

## 公開ルート（画面 / API）

### 画面構成

| 画面 | パス | 説明 | 認証ポリシー |
|------|------|------|-------------|
| Login | `/auth/login` | ログインページ | 不要 |
| AWS Dashboard | `/aws` | AWSダッシュボード | canAccess.dashboard |
| AWS Resources | `/aws/resources` | AWSリソース一覧 | canAccess.dashboard |
| AWS Costs | `/aws/costs` | コスト分析 | canAccess.dashboard |
| AWS High Cost Alerts | `/aws/alerts-high-cost` | 高コストアラート | canAccess.dashboard |
| AWS EKS Overview | `/aws/eks-overview` | EKS概要 | canAccess.dashboard |
| AWS EKS Systemlog | `/aws/eks-systemlog` | EKSシステムログ | canAccess.dashboard |
| AWS Admin Info | `/aws/admin-info` | AWS管理者情報 | canAccess.adminPage |
| K8s Dashboard | `/k8s` | K8sダッシュボード | canAccess.dashboard |
| Dashboard | `/dashboard` | ダッシュボードトップ | canAccess.dashboard |
| Microposts | `/dashboard/microposts` | マイクロポスト一覧 | canAccess.dashboard |
| Micropost New | `/dashboard/microposts/new` | マイクロポスト作成 | canAccess.dashboard |
| Micropost Show | `/dashboard/microposts/:id` | マイクロポスト詳細 | canAccess.dashboard |
| Settings | `/settings` | 設定管理画面 | canAccess.settingsRead |
| Admin | `/admin` | 管理画面 | canAccess.adminPage |
| Dev Tools | `/devtool` | 開発ツール画面 | canAccess.devtool |
| System Jobs | `/system/jobs` | ジョブ予約・ポーリング監視画面 | canAccess.system |

> ルート `/` は `/aws` にリダイレクト

### API（JSON）

| エンドポイント | メソッド | 認証 | 備考 |
|----------------|----------|------|------|
| `/api/health` | GET | 不要 | 公開CORS |
| `/api/info` | GET | 不要 | 公開CORS |
| `/auth/status` | GET | 不要 | AUTH FEATURE |
| `/auth/me` | GET | 任意 | AUTH FEATURE |
| `/aws/api/resources` | GET | 必要 | AWSリソース一覧 |
| `/aws/api/costs` | GET | 必要 | コストデータ取得 |
| `/k8s/api/pods` | GET | 必要 | Pod一覧 |
| `/k8s/api/nodes` | GET | 必要 | Node一覧 |
| `/k8s/api/metrics` | GET | 必要 | メトリクス取得 |
| `/dashboard/microposts/api` | GET | 必要 + apiKeyAuth | 一覧取得 |
| `/dashboard/microposts/api` | POST | 必要 + apiKeyAuth | 作成（multipart対応） |
| `/dashboard/microposts/api/:id` | GET | 必要 + apiKeyAuth | 詳細取得 |
| `/settings/api` | GET | 必要 | 設定取得 |
| `/settings/api` | POST | 必要 | 設定保存 |
| `/settings/api/history` | GET | 必要 | 設定履歴 |
| `/system/jobs/api` | GET | 必要 | ジョブ一覧 |
| `/system/jobs/api` | POST | 必要 | ジョブ作成 |
| `/system/jobs/api/:id` | GET | 必要 | ジョブ状態取得 |
| `/system/jobs/api/:id` | DELETE | 必要 | ジョブ削除 |

### 静的配信

| パス | 状態 |
|------|------|
| `GET /uploads/*` | 実装済み |
| `GET /favicon.svg` | 実装済み |

---

## Shared層 (src/shared/)

レイヤーに依存しない共通ユーティリティ。

| ファイル | 内容 |
|----------|------|
| `logger.ts` | Pino ロガー設定 |
| `errors.ts` | AppError クラス、Errors ファクトリー |
| `validators.ts` | validateBody() - Zodバリデーションヘルパー |
| `requestId.ts` | generateRequestId() |
| `authAuditLogger.ts` | 認証監査ログ (AUTH FEATURE) |

### Errors ファクトリー

| メソッド | ステータス | コード |
|----------|-----------|--------|
| `Errors.notFound(resource)` | 404 | NOT_FOUND |
| `Errors.badRequest(message)` | 400 | BAD_REQUEST |
| `Errors.unauthorized(message)` | 401 | UNAUTHORIZED |
| `Errors.forbidden(message)` | 403 | FORBIDDEN |
| `Errors.conflict(message)` | 409 | CONFLICT |
| `Errors.internal(message)` | 500 | INTERNAL_ERROR |
| `Errors.dbNotConfigured()` | 500 | DB_NOT_CONFIGURED |
| `Errors.dbUnavailable(context, cause)` | 503 | DB_UNAVAILABLE |
| `Errors.serviceUnavailable(service, cause)` | 503 | SERVICE_UNAVAILABLE |

---

## Domain層 (src/domain/)

### Entities

| Entity | ファイル | フィールド |
|--------|----------|-----------|
| Micropost | `entities/micropost.ts` | id, title, body, creatorId, imagePath, status, createdAt: Date, updatedAt: Date |
| Settings | `entities/settings.ts` | id, isCloud, isStub, aiModel, updatedAt: Date |
| Resource | `entities/resource.ts` | AWSリソース定義（EC2, RDS, S3等） |
| Cost | `entities/cost.ts` | コスト情報定義 |
| K8s | `entities/k8s.ts` | K8sリソース定義（Pod, Node, Deployment） |

### Repositories (Interface)

| Repository | メソッド |
|------------|----------|
| MicropostRepository | create, get, list, update, delete |
| SettingsRepository | get, save, getHistory |

### Services (Interface)

| Service | ファイル | メソッド | 備考 |
|---------|----------|----------|------|
| FileUploadService | `services/fileUpload.ts` | saveImage | Cloudinary経由、JPEG/PNG/GIF/WebP対応 |
| JobStore | `services/jobStore.ts` | create, get, list, update, delete | ジョブ状態管理 |

### Usecases

Use Caseはファクトリ関数パターンでDIコンテナに登録。

| UseCase | ファクトリ | メソッド |
|---------|-----------|----------|
| MicropostUseCases | `createMicropostUseCases` | create, get, list, update, delete |
| SettingsUseCases | `createSettingsUseCases` | get, save, getHistory |
| JobUseCases | `createJobUseCases` | create, get, list, update, delete |

ControllerはRepositoryではなくUseCasesを受け取る設計。

### Auth Domain

| ファイル | 内容 |
|----------|------|
| `auth/types.ts` | Role型、ROLE_HIERARCHY、AuthContext、PolicyFunction |
| `auth/policies.ts` | hasMinRole, hasAnyRole, hasRole, allOf, anyOf, not, canAccess |
| `auth/roleResolver.ts` | resolveRole, buildAuthContext |
| `auth/index.ts` | モジュールエクスポート |

---

## Contracts層 (src/contracts/)

API契約の長期安定性（LTS: Long Term Support）を保証するための型定義層。

### 概要

| 項目 | 説明 |
|------|------|
| 目的 | APIレスポンス形式の後方互換性保証 |
| バージョン | lts-v1（破壊的変更時はv2を新設） |
| 凍結ルール | 公開後の型定義は変更不可 |

### ディレクトリ構成

```
contracts/lts-v1/
├── documents/           # 凍結された型定義
│   ├── micropost.ts     # LtsMicropostDocument, LtsMicropostCreateInput
│   ├── settings.ts      # LtsSettingsDocument, LtsSettingsInput
│   └── index.ts
├── types/               # 共通型定義
│   ├── ids.ts           # MicropostId, SettingsId, UserId
│   ├── time.ts          # LtsTimestamp, TimePolicy
│   ├── common.ts        # LtsDocumentBase, LtsMicropostStatus
│   └── index.ts
├── adapters/            # ドメイン ↔ 契約変換
│   ├── errors.ts        # AdaptError
│   ├── micropost.ts     # adaptMicropostToLtsV1, adaptLtsV1ToMicropost
│   ├── settings.ts      # adaptSettingsToLtsV1, adaptLtsV1ToSettings
│   └── index.ts
└── index.ts             # 公開API
```

### Documents（凍結型）

| Document | フィールド |
|----------|-----------|
| LtsMicropostDocument | id, title, body, creatorId, imagePath?, status, createdAt, updatedAt |
| LtsSettingsDocument | id, isCloud, isStub, aiModel, updatedAt |

### Types

| 型 | 説明 |
|-----|------|
| MicropostId | マイクロポストID（ObjectId形式） |
| SettingsId | 設定ID（timestamp-random形式） |
| UserId | ユーザーID（Clerk形式） |
| LtsTimestamp | Date型保証のタイムスタンプ |
| LtsMicropostStatus | 'draft' \| 'published' |

### Adapters

| 関数 | 説明 |
|------|------|
| `adaptMicropostToLtsV1` | Micropost → LtsMicropostDocument |
| `adaptLtsV1ToMicropost` | LtsMicropostDocument → Micropost |
| `adaptSettingsToLtsV1` | Settings → LtsSettingsDocument |
| `adaptLegacySettingsToLtsV1` | レガシー形式対応変換 |

### 使用方法

```typescript
import { adaptSettingsToLtsV1 } from '../contracts/lts-v1'

// コントローラでの使用例
const settings = await useCases.get()
const response = adaptSettingsToLtsV1(settings)
return c.json({ success: true, data: response })
```

---

## Infrastructure層 (src/infra/)

### Database Clients

| Client | ファイル | 関数 |
|--------|----------|------|
| MongoDB | `database/clients/mongo.ts` | createMongoClient |
| Redis | `database/clients/redis.ts` | createRedisClient (Upstash) |

### Database Utilities

| ファイル | 関数 | 説明 |
|----------|------|------|
| `database/errors.ts` | wrapDbOperation | DB操作をラップしAppErrorに変換 |

### Cache

| ファイル | 説明 |
|----------|------|
| `cache/memoryCache.ts` | インメモリキャッシュ（TTL付き） |

### Repositories (Implementation)

| Repository | Backend | コレクション |
|------------|---------|-------------|
| MicropostRepository | MongoDB | microposts |
| SettingsRepository | MongoDB | settings, settings_history |

Settings履歴は最新10件のみ保持。

### Services (Implementation)

| Service | ファイル | 備考 |
|---------|----------|------|
| FileUploadService | `services/fileUpload.ts` | Cloudinary v2 API使用 |
| InMemoryJobStore | `services/jobStore/memory.ts` | 開発/テスト用（再起動で消失） |
| RedisJobStore | `services/jobStore/redis.ts` | Upstash Redis（永続化、24時間TTL） |

対応MIME (FileUpload): JPEG, PNG, GIF, WebP

---

## Presentation層 (src/presentation/)

### Routes構成

機能ごとに `page.ts` (画面) と `api.ts` (JSON API) を分離:

```
routes/
├── api/health.ts         # 公開API
├── auth/
│   ├── page.ts           # ログインページ
│   ├── api.ts            # 認証API
│   └── index.ts          # 統合
├── aws/
│   ├── page.ts           # AWS画面
│   ├── api.ts            # AWS API
│   └── index.ts
├── k8s/
│   ├── page.ts           # K8s画面
│   ├── api.ts            # K8s API
│   └── index.ts
├── dashboard/
│   ├── index.ts
│   └── micropost/
│       ├── page.ts       # 一覧/詳細/新規ページ
│       ├── api.ts        # CRUD API
│       └── index.ts
├── settings/
│   ├── page.ts
│   ├── api.ts
│   └── index.ts
└── system/jobs/
    ├── page.ts
    ├── api.ts
    └── index.ts
```

### Middleware

| Middleware | ファイル | 備考 |
|------------|----------|------|
| rateLimiter | `middleware/rateLimiter.ts` | hono-rate-limiter（環境変数で設定可能） |
| requestLogger | `middleware/requestLogger.ts` | リクエストID生成、レスポンス時間ログ |
| errorHandler | `middleware/errorHandler.ts` | AppError、API/ページ判別 |
| apiKeyAuth | `middleware/apiKeyAuth.ts` | `INTERNAL_PROJECT_API_KEY`設定時のみ有効 |
| publicCors | `middleware/cors.ts` | origin: * |
| protectedCors | `middleware/cors.ts` | credentials: true, CORS_ORIGINS対応 |
| authMiddleware | `middleware/auth/clerk.ts` | Clerkセッション検証 (AUTH FEATURE) |
| requireAuth | `middleware/auth/clerk.ts` | 認証必須 (AUTH FEATURE) |
| authContext | `middleware/auth/clerk.ts` | AuthContext構築 (AUTH FEATURE) |
| authorize | `middleware/auth/clerk.ts` | ポリシーベース認可 (AUTH FEATURE) |
| authHelpers | `middleware/auth/helpers.ts` | authRequired(), authOptional() |

### エラーハンドリング

| 機能 | 状態 |
|------|------|
| AppErrorクラス | 実装済み (`shared/errors.ts`) |
| Errorsファクトリー | 実装済み（9種類） |
| API/ページ判別 | 実装済み（APIはJSON、ページはHTML） |
| リクエストID | 実装済み（X-Request-Id） |
| 本番環境詳細隠蔽 | 実装済み |

### Views (EJSテンプレート)

| ディレクトリ | 内容 |
|--------------|------|
| layouts/ | メインレイアウト（main.ejs、aws-main.ejs） |
| auth/ | ログインページ (AUTH FEATURE) |
| aws/ | AWSダッシュボード（dashboard, resources, costs, alerts-high-cost, eks-overview, eks-systemlog, admin-info） |
| k8s/ | K8sダッシュボード |
| dashboard/ | ダッシュボードトップ |
| dashboard/micropost/ | マイクロポスト一覧/詳細/作成/編集 |
| settings/ | 設定管理 |
| admin/ | 管理画面 |
| devtool/ | 開発ツール |
| system/jobs/ | ジョブ管理 |

### Helpers

| ファイル | 内容 |
|----------|------|
| `helpers/render.ts` | EJSレンダリングヘルパー（認証情報自動注入） |
| `helpers/awsFormatters.ts` | AWSデータフォーマッター（リソース・コスト表示用） |
| `helpers/pageController.ts` | ページコントローラファクトリ、notFoundPage(), forbiddenPage() |
| `helpers/apiController.ts` | APIコントローラファクトリ（レスポンス形式統一） |

### Schemas

| ファイル | 内容 |
|----------|------|
| `schemas/micropost.ts` | createMicropostSchema |
| `schemas/settings.ts` | settingsSchema |

---

## DI Container (src/di/)

### 設計方針

- **InjectionMode.CLASSIC**: 関数中心のため明示的 `resolve()` を採用
- **Factory不使用**: 全生成処理を `container.ts` に集約
- **ライフサイクル**: 非同期リソース（mongoClient）の破棄は呼び出し側で管理

### 登録済み依存（Cradle）

| 名前 | 型 | 備考 |
|------|-----|------|
| mongoClient | MongoClient | |
| redisClient | Redis \| null | Optional |
| micropostRepo | MicropostRepository | 内部用 |
| settingsRepo | SettingsRepository | 内部用 |
| fileUploadService | FileUploadService | 内部用 |
| jobStore | JobStore | Redis優先、なければInMemory |
| micropostUseCases | MicropostUseCases | Controller向け |
| settingsUseCases | SettingsUseCases | Controller向け |
| jobUseCases | JobUseCases | Controller向け |

---

## テスト

### 概要

| カテゴリ | ファイル数 | テスト数 |
|----------|-----------|---------|
| ユニットテスト | 51 | 200+ |
| 統合テスト | 3 | 17 |
| E2Eテスト | 1 | - |
| **合計** | **55** | **217+** |

### ユニットテスト (test/unit/)

| ディレクトリ | 対象 | ファイル数 |
|--------------|------|-----------|
| auth/ | ポリシー、ロール解決 | 2 |
| aws/ | AWSリソース、フォーマッター、キャッシュ | 3 |
| controllers/ | 各種コントローラ | 7 |
| di/ | DIコンテナ | 1 |
| env/ | 環境設定 | 2 |
| helpers/ | ヘルパー関数 | 3 |
| infra/ | インフラ層実装 | 6 |
| middleware/ | ミドルウェア | 7 |
| routes/ | ルート | 9 |
| schemas/ | バリデーションスキーマ | 2 |
| services/ | サービス実装 | 2 |
| shared/ | 共通ユーティリティ | 5 |
| usecases/ | ユースケース | 2 |

### 統合テスト (test/integration/)

| ファイル | 対象 |
|----------|------|
| index.test.ts | ルート、ヘルスチェック |
| micropost.test.ts | マイクロポストAPI |
| settings.test.ts | 設定API |

### E2Eテスト (e2e/)

| ファイル | 対象 |
|----------|------|
| example.spec.ts | 基本的なページ・API確認 |

### テストヘルパー

| ファイル | 内容 |
|----------|------|
| helpers/container.ts | テスト用DIコンテナ（モックリポジトリ付き） |

---

## 削除可能な機能

### AUTH FEATURE (Clerk認証)

削除手順は [02-clerk認証削除.md](../04-機能ガイド/02-clerk認証削除.md) 参照。

- `src/domain/auth/`
- `src/presentation/middleware/auth/`
- `src/presentation/routes/auth/`
- `src/presentation/views/auth/`
- `src/shared/authAuditLogger.ts`
- 環境変数: `AUTH_ENABLED`, `AUTH_CLERK_SECRET_KEY`, `AUTH_CLERK_PUBLISHABLE_KEY`, `OWNER_ADMIN_USER_IDS`

---

## 未実装・TODO

| 項目 | 状態 |
|------|------|
| JWT認証 | 未実装（Clerk使用を推奨） |
| Admin機能 | UIのみ（API未実装） |
| DBマイグレーション | 未実装（スキーマはリポジトリ内で暗黙的） |
| API仕様書 | 未実装（Swagger/OpenAPI） |

---

## 関連ドキュメント

- [../../README.md](../../README.md) - 構築・運用ガイド
- [../04-機能ガイド/01-clerk認証.md](../04-機能ガイド/01-clerk認証.md) - Clerk認証の使い方
- [../04-機能ガイド/02-clerk認証削除.md](../04-機能ガイド/02-clerk認証削除.md) - Clerk認証の削除方法
- [../04-機能ガイド/03-sample機能削除.md](../04-機能ガイド/03-sample機能削除.md) - サンプルコード削除方法（参照用）
- [../05-セットアップ/01-doppler.md](../05-セットアップ/01-doppler.md) - Doppler連携ガイド
