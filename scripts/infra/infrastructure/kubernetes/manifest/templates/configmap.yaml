apiVersion: v1
kind: ConfigMap
metadata:
  name: {{name}}
  namespace: {{namespace:default}}
{{#if labels}}
  labels:
{{#each labels}}
    {{@key}}: "{{@value}}"
{{/each}}
{{/if}}
data:
{{#each data}}
  {{@key}}: "{{@value}}"
{{/each}}
