apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # ApplicationSet (環境別に自動生成)
  - app-appset.yaml
  - cnpg-appset.yaml
  - monitoring-appset.yaml
  # 旧 Application (ApplicationSet 移行後は削除可能)
  # - apps-local.yaml
  # - cnpg-local.yaml
