apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: app
  namespace: argocd
spec:
  generators:
    - list:
        elements:
          # - env: staging
          #   cluster: https://eks-staging.example.com
          #   namespace: app
          - env: prod
            cluster: https://kubernetes.default.svc
            namespace: app
  template:
    metadata:
      name: 'app-{{env}}'
      namespace: argocd
    spec:
      project: default
      source:
        repoURL: git@github.com:kurosawa-kuro/eks-app-mlops-platform.git
        targetRevision: HEAD
        path: 'infra/k8s/apps/overlays/{{env}}'
      destination:
        server: '{{cluster}}'
        namespace: '{{namespace}}'
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
          - CreateNamespace=true
