apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - operator-install.yaml
  - secrets.yaml
  - cluster.yaml

labels:
  - pairs:
      app.kubernetes.io/part-of: eks-app-mlops-platform
      app.kubernetes.io/managed-by: kustomize
    includeSelectors: false
