# Strategic merge patch to convert ClusterIP to NodePort
apiVersion: v1
kind: Service
metadata:
  name: monolith-service
  namespace: monolith
spec:
  type: NodePort
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      nodePort: 30080
