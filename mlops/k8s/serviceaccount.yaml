apiVersion: v1
kind: ServiceAccount
metadata:
  name: mlops-sa
  namespace: mlops
  labels:
    app.kubernetes.io/name: mlops-sa
    app.kubernetes.io/part-of: mlops-pipeline
  annotations:
    # IRSA annotation - role ARN will be set via Makefile from terraform output
    eks.amazonaws.com/role-arn: "PLACEHOLDER_ROLE_ARN"
